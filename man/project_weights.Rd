% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraint_fn_map.R
\name{project_weights}
\alias{project_weights}
\title{Deterministic constraint repair via Dykstra's alternating projection}
\usage{
project_weights(
  w,
  min_sum,
  max_sum,
  min_box,
  max_box,
  groups = NULL,
  cLO = NULL,
  cUP = NULL,
  max_iter = 1000,
  tol = 1e-10
)
}
\arguments{
\item{w}{numeric vector of weights to project}

\item{min_sum}{scalar lower bound on sum(w)}

\item{max_sum}{scalar upper bound on sum(w)}

\item{min_box}{numeric vector of lower bounds per asset}

\item{max_box}{numeric vector of upper bounds per asset}

\item{groups}{list of integer vectors specifying group membership (or NULL)}

\item{cLO}{numeric vector of group lower bounds (or NULL)}

\item{cUP}{numeric vector of group upper bounds (or NULL)}

\item{max_iter}{maximum number of Dykstra cycles (default 1000)}

\item{tol}{convergence tolerance (default 1e-10)}
}
\value{
projected weights satisfying all constraints, or NULL if the
  algorithm fails to converge (suggesting an empty intersection / conflicting
  constraints)
}
\description{
Finds the nearest (Euclidean) feasible point to \code{w} in the intersection
of convex constraint sets: box constraints, weight-sum constraint, and
group-sum constraints. Each individual group is treated as a separate
convex set with its own Dykstra residual, correctly handling overlapping
groups.
}
\details{
When the algorithm detects that \code{x} has stabilized (changes below
\code{tol}) but the result is not yet feasible, it continues iterating —
this handles limit-cycle cases where residuals are still evolving.
Returns \code{NULL} if \code{max_iter} is reached without finding a
feasible point (suggesting conflicting constraints or an extremely
challenging geometry).
}
