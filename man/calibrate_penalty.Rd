% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constrained_objective.R
\name{calibrate_penalty}
\alias{calibrate_penalty}
\title{Calibrate penalty parameter for constrained optimization}
\usage{
calibrate_penalty(
  R,
  portfolio,
  env = NULL,
  ...,
  n_pilot = 20,
  scaling_factor = 10000,
  min_penalty = 1
)
}
\arguments{
\item{R}{an xts, vector, matrix, data frame, timeSeries or zoo object of
asset returns}

\item{portfolio}{an object of class \code{portfolio} specifying the
constraints and objectives for the optimization}

\item{env}{optional list of pre-computed moments (as returned by
\code{momentFUN}). If \code{NULL}, moments are recomputed internally.}

\item{\dots}{additional arguments (currently unused, reserved for future use)}

\item{n_pilot}{number of pilot random portfolios to generate (default 20)}

\item{scaling_factor}{multiplier applied to the observed objective scale
(default 10000). A value of 10000 ensures that a 1\% constraint violation
produces a penalty term roughly 100x the typical objective value.}

\item{min_penalty}{floor value for the returned penalty (default 1)}
}
\value{
a single positive numeric penalty value
}
\description{
Estimates an appropriate penalty value by evaluating the objective function
on a small pilot set of random portfolios with \code{penalty=0} (zeroing out
constraint penalty contributions). The returned penalty is scaled relative
to the observed magnitude of objective values, ensuring constraint violations
dominate the objective without overwhelming numerical precision.
}
\details{
The default penalty of \code{1e4} used by \code{\link{constrained_objective}}
is arbitrary and can cause problems when the objective function returns values
on a very different scale. For small-scale objectives (e.g., daily variance
~1e-4), the penalty dwarfs the objective, causing stochastic solvers to ignore
it. For large-scale objectives, the penalty may be insufficient.
}
\seealso{
\code{\link{constrained_objective}}, \code{\link{optimize.portfolio}}
}
